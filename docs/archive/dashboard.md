This report is now scoped to the Scraped pipeline as a whole rather than a single core member, so it can support dashboard-level owner filtering.

The top-line KPI view shows total leads assigned, calls attempted, and calls connected and interested across the full Scraped pipeline. 

GMeet fixed remains a separate KPI in the same pipeline scope. 

Leads can now be compared across core members instead of being hardcoded to Shreyas. 

The same owner-agnostic version also keeps Scraped Type visible so you can compare ITservices and Distressed startups. 

The cumulative trend by core member supports the dashboard version when you want to switch from one owner to another. 

The cumulative trend by Scraped Type shows how ITservices and Distressed startups contribute over time. 

Calls attempted are also tracked cumulatively over time for the full Scraped pipeline. 

Interested and GMeet progression are shown as cumulative pipeline movement trends in the same owner-agnostic version. 